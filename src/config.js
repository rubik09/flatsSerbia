export const regexSearchNewFlats = /<a[^>]*class="product-title"[^>]*href="([^"]+)"[^>]*>/g;

export const regexInfoForFlat = /QuidditaEnvironment\.CurrentClassified\s*=\s*({.*?});/;

export const { BOTID, CHANNELID } = process.env;
